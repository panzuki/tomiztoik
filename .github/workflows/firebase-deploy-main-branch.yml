name: Firebase Hosting 自動デプロイ (Vue 3/Tailwind対応)

on:
  push:
    branches:
      - main # mainブランチへのプッシュをトリガー

jobs:
  deploy:
    # 実行環境を指定
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production # ビルド時の環境変数を設定

    steps:
      - name: 🚀 コードのチェックアウト
        uses: actions/checkout@v4

      - name: 💻 Node.jsのセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' 
          
      # ステップ 1: 依存関係のクリーンインストール
      - name: 📦 依存関係のクリーンインストール
        # npm ci を実行し、必要なパッケージをインストール
        run: |
          rm -rf node_modules dist 
          npm ci

      # ステップ 2: プロジェクトのビルドを実行 (run-pを回避し、distを生成)
      # type-checkとbuild-onlyを個別のコマンドとして実行
      - name: 🔨 プロジェクトのビルドを実行
        run: |
          npm run type-check 
          npm run build-only 
        
      - name: 📂 ビルド生成物（dist）を確認
        # distフォルダが生成されているかを確認
        run: ls -R dist 
        
      - name: 🔥 Firebase Hostingへデプロイ
        # Firebase Extended のデプロイアクションを使用
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TOMIZBASE }} 
          channelId: live
          projectId: tomizbase
          
          # アクションの実行起点をリポジトリのルート ( . ) に設定。
          entryPoint: .

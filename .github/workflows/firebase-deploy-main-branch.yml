name: Firebase Hosting 自動デプロイ (Vue 3/Tailwind対応)

on:
  push:
    branches:
      - main # mainブランチへのプッシュをトリガー

jobs:
  deploy:
    # 実行環境を指定
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production # ビルド時の環境変数を設定

    steps:
      - name: 🚀 コードのチェックアウト
        uses: actions/checkout@v4

      - name: 💻 Node.jsのセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' 

      # ステップ分離 1: 依存関係のクリーンインストール (ファイルをインストール)
      - name: 📦 依存関係のクリーンインストール
        run: |
          # 以前の残骸を削除してからクリーンインストール
          rm -rf node_modules dist 
          npm ci

      # ステップ分離 2: プロジェクトのビルドを実行 (run-pエラー回避のためステップを分離)
      - name: 🔨 プロジェクトのビルドを実行
        run: npm run build 
        
      - name: 📂 ビルド生成物（dist）を確認
        # ビルドが成功し、distフォルダが生成されたかを確認
        run: ls -R dist 
        
      - name: 🔥 Firebase Hostingへデプロイ
        # Firebase Extended のデプロイアクションを使用
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          # Firebase サービスアカウントキー (GitHub Secretsに登録済み)
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TOMIZBASE }} 
          # 常に live チャンネル (本番環境) にデプロイ
          channelId: live
          # プロジェクト ID を指定
          projectId: tomizbase
          
          # アクションの実行起点をリポジトリのルート ( . ) に設定。
          entryPoint: .
